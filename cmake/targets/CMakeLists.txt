cmake_minimum_required(VERSION 3.12)
project(targets LANGUAGES C)
add_executable(gms gms.c)
if("${BUILD_TARGET}" STREQUAL "gtam")
    message("include GTAM sources")
else()
    message("include GTLT soruces")
endif()
